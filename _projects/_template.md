---
layout: page
title: Short project name
description: One sentence that says what the project does and who it is for.
img: assets/img/publication_preview/YOUR_IMAGE.png # the card thumbnail, or leave blank
importance: 1 # cards sort by this number inside a category, lowest first
category: research software # must match one entry of display_categories in _pages/projects.md
github: https://github.com/YOUR-USER/YOUR-REPO
# redirect: https://example.com  # set this to send the card straight to an external page
# related_publications: true     # set this to render the {% cite key %} entries below
---

Jekyll ignores any file in this folder whose name starts with an underscore, so this
template never appears on the site. Copy it to `my-project.md` to start a new project page.

## What it does

Write two or three sentences here.

## How to use it

Show the shortest command that gets a result.

```bash
pip install your-package
```
